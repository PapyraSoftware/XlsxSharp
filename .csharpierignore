# Vendored sources. XlsxSharp.Parser/ is imported from ClosedXML/ClosedXML.Parser with its full
# history and is kept formatted the way upstream formats it, so that pulling upstream changes stays
# a merge instead of a reformat. Reformatting it with CSharpier would rewrite every file and turn
# every future subtree pull into a conflict.
XlsxSharp.Parser/
