# Leftover from the time XlsxSharp.Parser/ was a git subtree of ClosedXML/ClosedXML.Parser and
# had to stay formatted the way upstream formatted it. It is first-party code now and nothing is
# pulled into it any more, so this exclusion can go, but lifting it reformats every file in the
# directory and that belongs in a commit of its own. See CONTRIBUTING.md.
XlsxSharp.Parser/
